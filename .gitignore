# Rust
target/
Cargo.lock
**/*.rs.bk
__pycache__/
*.py[cod]

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
# .cargo/config.toml 是项目构建配置（xtask 别名、wasm target 的 --cfg wasm_js 与
# panic=abort），不是机器本地设置：不入库会让别人 clone 后 wasm 构建行为与本仓不一致，
# 架构守卫测试也读不到它。只忽略真正本地的缓存产物。
.cargo/registry/
.cargo/git/
platforms/

# OS
.DS_Store
Thumbs.db

# Build
*.so
*.dylib
*.dll
*.a
*.sh
*.db
*db-*
*.log

doc/
certs/
examples/target/
examples/temp-data/
examples/certs/
dist/
node_modules/
bindings/wasm/pkg/

examples/flare/
examples/tauri/

# Data directories (database and logs)
chat-data/
temp-data/

# Tauri ignore file (for file watching)
.taurignore

*.zip
